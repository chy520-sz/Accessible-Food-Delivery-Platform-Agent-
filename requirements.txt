# ==================== Web 框架 ====================
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19

# ==================== HTTP 客户端（含异步支持） ====================
httpx==0.28.1

# ==================== LangChain 1.x 生态 ====================
# v1 标准 Agent 入口 create_agent 由 langchain 提供；短期记忆由 langgraph checkpointer 管理
langchain==1.2.13
langchain-core==1.2.18
langchain-openai==1.1.11
# create_agent 依赖 langgraph（进程内 InMemorySaver checkpointer）
langgraph>=1.1.1,<1.2
# 显式声明实际使用的文本分块器
langchain-text-splitters>=1,<2

# ==================== 向量数据库 Milvus ====================
# 直接使用官方 MilvusClient，避免依赖旧 ORM Collection 连接别名。
pymilvus>=2.6,<2.7

# ==================== 语音合成 ====================
# edge-tts: 微软免费 TTS，无需 API Key
edge-tts==7.2.8

# ==================== 工具库 ====================
# 不再手动单独约束 pydantic-core，由对应 Pydantic 版本自行管理
pydantic>=2.10
python-dotenv==1.0.1
tenacity==9.0.0
